# Project Baseline
This is a React.js based web application, currently configured to be deployed on Firebase Hosting. The project includes a standard `src`/`public` setup, is managed via `npm`, and utilizes GitHub Actions for CI/CD workflows.

## Agent Memory Strategy
You must maintain the accuracy of this rules.md file. Whenever we finalize a new architectural decision, update a database schema, implement a new API route, or modify our deployment infrastructure (such as K3s manifests, Ansible playbooks, or CI/CD pipelines), you are strictly required to silently update this file to reflect the new state before completing the task.

## Execution Protocol
Before making sweeping multi-file changes or executing complex infrastructure refactoring, you must first generate an Artifact (an implementation plan or code diff) for user approval to minimize token waste and avoid destructive errors.
